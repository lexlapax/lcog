# Node.js
node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.vercel
coverage/

# Dependencies
# Go modules automatically handle dependencies via go.mod and go.sum
# The vendor directory is generally not committed with Go modules
vendor/

# Environment variables / Secrets
# NEVER commit files containing secrets like API keys!
.env

# Configuration Files (Optional)
# If your config.yaml (or other config files) might contain sensitive data
# or local overrides, ignore them. Commit them ONLY if they contain
# non-sensitive defaults safe for all developers/environments.
config.yaml


# Log files
*.log
*.log.*

# Test and Coverage Data
coverage.*
profile.*
*.prof
*.out

# IDE / Editor directories and files
.vscode/
.idea/
*.iml
*.sublime-project
*.sublime-workspace
*.DS_Store
.aider*

# databases
*.db

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini

# Build Session Data (GoLand specific, others might exist)
.build/

# Go workspace file (Go 1.18+)
go.work

# Any local build output directories you might create
/build/
/bin/
/dist/


# cogmem files
.cogmem_history
cogmem-go/.cogmem_history
cogmem-go/bin/example-client
cogmem-go/test/scripts/output/
